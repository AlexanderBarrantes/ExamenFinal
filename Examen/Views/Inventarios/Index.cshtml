@model IEnumerable<Modelos.Modelos.Inventarios>

@{
    ViewBag.Title = "Index";
}

<h2>Inventarios</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Producto)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Cantidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CantidadMinima)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CantidadMaxima)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GravadoExento)
                    </th>
        <th>
            @Html.DisplayNameFor(model => model.Precio)

        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Producto)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cantidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CantidadMinima)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CantidadMaxima)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GravadoExento)
        </td>
        <th>
            @Html.DisplayFor(model => item.Precio)

        </th>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
            @Html.ActionLink("Details", "Details", new { id=item.id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id })
        </td>
    </tr>
}

</table>
